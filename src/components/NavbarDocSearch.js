import DocSearchBox from './DocSearch/DocSearchBox';

/** Navbar documentation search — shared DocSearch UX. */
export default function NavbarDocSearch() {
  return <DocSearchBox id="navbar-docs-search" variant="navbar" />;
}
